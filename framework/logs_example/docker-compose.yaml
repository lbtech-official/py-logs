version: '3.8'
services:
    web:
        build: .
        container_name: django_logs
    nginx:
        image: nginx:latest
        container_name: nginx_logs
        depends_on:
            - web
        ports:
            - "8000:8000"
        volumes:
            - ./conf/nginx.conf:/etc/nginx/nginx.conf